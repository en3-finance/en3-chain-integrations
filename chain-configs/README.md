# Chain Configs

This directory contains public EVM-compatible network configuration templates.

Use placeholders until official public network parameters are selected. Do not commit private RPC credentials, customer-specific settings, API keys, or internal access details.

